<!DOCTYPE html>
<html>
  <head>
    <title>MFilmes</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= javascript_importmap_tags %>
    <!-- Bootstrap CSS (via CDN) -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    >

    <!-- Bootstrap JS (via CDN) -->
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
      crossorigin="anonymous"
      defer
    ></script>
  </head>

  <body class="bg-black text-white">
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
     

        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav me-auto">
            <li class="nav-item">
              <%= link_to '🎬 MFilmes', movies_path, class: 'nav-link text-success fw-semibold' %>
            </li>
          </ul>
          
          <ul class="navbar-nav ms-auto">
            <% if logged_in? %>
              <!-- Usuário logado - Mostrar menu de perfil -->
              <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle text-success fw-semibold" href="#" role="button" data-bs-toggle="dropdown">
                  👤 <%= current_user.name %>
                </a>
                <ul class="dropdown-menu dropdown-menu-end">
                  <li><%= link_to "Minha Conta", profile_path, class: "dropdown-item text-dark" %></li>
                  <li><%= link_to "Editar Perfil", edit_user_path(current_user), class: "dropdown-item text-dark" %></li>
                  <li><hr class="dropdown-divider"></li>
                  <li><%= link_to "Sair", logout_path, method: :delete, class: "dropdown-item text-dark" %>
                </ul>
              </li>
            <% else %>
              <!-- Usuário deslogado - Mostrar opções de login -->
              <li class="nav-item">
                <%= link_to 'Entrar', login_path, class: 'nav-link text-success fw-semibold' %>
              </li>
              <li class="nav-item">
                <%= link_to 'Cadastrar', signup_path, class: 'nav-link text-success fw-semibold' %>
              </li>
            <% end %>
          </ul>
        </div>
      </div>
    </nav>

    <!-- Conteúdo com padding no topo -->
    <div class="container mt-5 pt-4">
      <% if notice %>
        <div class="alert alert-success alert-dismissible fade show" role="alert">
          <%= notice %>
          <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
      <% end %>
      
      <% if alert %>
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
          <%= alert %>
          <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
      <% end %>
      
      <%= yield %>
    </div>
    
<ul class="navbar-nav me-auto">
  <li class="nav-item">
    <%= link_to '', movies_path, class: 'nav-link text-success fw-semibold' %>
  </li>
</ul>
<ul class="navbar-nav me-auto">
  <%= link_to "Adicionar um Filme", new_movie_path, class: "btn btn-success mt-3" %>
        
</ul>
  </body>
</html>