<div class="row justify-content-center mt-5">
  <div class="col-md-6">
    <div class="card bg-dark text-white border-success">
      <div class="card-header bg-success text-dark">
        <h2 class="text-center mb-0">🎬 Entrar no MFilmes</h2>
      </div>
      <div class="card-body">
        <%= form_with url: login_path, local: true do |form| %>
          <div class="mb-3">
            <%= form.label :email, "Email", class: "form-label text-success fw-semibold" %>
            <%= form.email_field :email, class: "form-control bg-black text-white border-success", required: true, placeholder: "seu@email.com" %>
          </div>
          
          <div class="mb-3">
            <%= form.label :password, "Senha", class: "form-label text-success fw-semibold" %>
            <%= form.password_field :password, class: "form-control bg-black text-white border-success", required: true, placeholder: "Sua senha" %>
          </div>
          
          <div class="d-grid">
            <%= form.submit "Entrar", class: "btn btn-success btn-lg fw-bold" %>
          </div>
        <% end %>
        
        <div class="mt-3 text-center">
          <p class="text-muted">Não tem conta? 
            <%= link_to "Cadastre-se aqui", signup_path, class: "text-success fw-semibold" %>
          </p>
        </div>
      </div>
    </div>
  </div>
</div>