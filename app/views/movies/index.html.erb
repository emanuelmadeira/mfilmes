<!-- app/views/movies/index.html.erb -->
<div class="container mt-4">
  <div class="d-flex justify-content-between align-items-center mb-4">
    <h2 class="text-success">🎬 Meus Filmes</h2>
    <% if logged_in? %>
      <%= link_to "➕ Adicionar Filme", new_movie_path, class: "btn btn-success" %>
    <% end %>
  </div>

  <% if @movies.empty? %>
    <div class="card bg-dark text-white border-success">
      <div class="card-body text-center py-5">
        <h4 class="text-muted">Nenhum filme cadastrado ainda</h4>
        <% if logged_in? %>
          <p class="text-muted">Seja o primeiro a adicionar um filme!</p>
          <%= link_to "Adicionar Primeiro Filme", new_movie_path, class: "btn btn-success mt-3" %>
        <% else %>
          <p class="text-muted">Faça login para adicionar filmes</p>
          <%= link_to "Fazer Login", login_path, class: "btn btn-success mt-3" %>
        <% end %>
      </div>
    </div>
  <% else %>
    <!-- Lista Vertical - Um filme por linha em todos os dispositivos -->
    <div class="row">
      <% @movies.each do |movie| %>
        <div class="col-12 col-md-6 col-lg-4 mb-4">
          <div class="card bg-dark text-white border-success h-100 movie-card-mobile">
            <div class="row g-0">
              <!-- Imagem do Filme -->
              <div class="col-md-3 movie-image-container">
                <% if movie.image.attached? %>
                  <%= image_tag movie.image, class: "img-fluid w-100 h-100 movie-image", alt: movie.title %>
                <% else %>
                 <div class="movie-placeholder d-flex align-items-center justify-content-center">
                    <span class="fs-1 text-muted">🎬</span>
                 </div>
                <% end %>
              </div>

              
              <!-- Conteúdo do Filme -->
              <div class="col-md-9">
                <div class="card-body">
                  <div class="d-flex justify-content-between align-items-start mb-3">
                    <h5 class="card-title text-success mb-0"><%= movie.title %></h5>
                    <div class="d-flex align-items-center gap-2">
                      <span class="badge bg-success"><%= movie.release_year %></span>
                      <small class="text-light">💬 <%= movie.comentarios_count %></small>
                    </div>
                  </div>
                  
                  <div class="row">
                    <div class="col-sm-6">
                      <p class="card-text mb-1">
                        <small class="text-success">Diretor:</small><br>
                        <span class="text-light"><%= movie.diretor || "Não informado" %></span>
                      </p>
                      <p class="card-text mb-1">
                        <small class="text-success">Gênero:</small><br>
                        <span class="text-light"><%= movie.genre || "Não especificado" %></span>
                      </p>
                    </div>
                    <div class="col-sm-6">
                      <p class="card-text mb-1">
                        <small class="text-success">Duração:</small><br>
                        <span class="text-light"><%= movie.duracao || "Não informada" %></span>
                      </p>
                      <p class="card-text mb-1">
                        <small class="text-success">Adicionado por:</small><br>
                        <span class="text-warning"><%= movie.user.name %></span>
                      </p>
                    </div>
                  </div>
                  
                  <% if movie.description.present? %>
                    <p class="card-text mt-2">
                      <small class="text-success">Descrição:</small><br>
                      <span class="text-light"><%= truncate(movie.description, length: 150) %></span>
                    </p>
                  <% end %>
                  
                  <div class="mt-3">
                    <div class="d-flex flex-wrap gap-2">
                      <%= link_to "👀 Ver Detalhes", movie_path(movie), class: "btn btn-outline-info btn-sm" %>
                      <% if logged_in? && current_user == movie.user %>
                        <%= link_to "✏️ Editar", edit_movie_path(movie), class: "btn btn-outline-warning btn-sm" %>
                        <%= button_to "🗑️ Excluir", movie_path(movie), method: :delete, 
                            class: "btn btn-outline-danger btn-sm",
                            data: { confirm: "Tem certeza que deseja excluir #{movie.title}?" } %>
                      <% end %>
                    </div>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      <% end %>
    </div>
    
    <!-- Paginação -->
    <!-- Paginação com classe personalizada -->
<div class="d-flex justify-content-center mt-4">
  <nav>
    <ul class="pagination pagination-custom">
      <% if @movies.current_page > 1 %>
        <li class="page-item">
          <%= link_to "‹", url_for(page: @movies.current_page - 1), class: "page-link" %>
        </li>
      <% else %>
        <li class="page-item disabled">
          <span class="page-link">‹</span>
        </li>
      <% end %>
      
      <% (@movies.current_page-2..@movies.current_page+2).each do |page| %>
        <% if page > 0 && page <= @movies.total_pages %>
          <li class="page-item <%= 'active' if page == @movies.current_page %>">
            <%= link_to page, url_for(page: page), class: "page-link" %>
          </li>
        <% end %>
      <% end %>
      
      <% if @movies.current_page < @movies.total_pages %>
        <li class="page-item">
          <%= link_to "›", url_for(page: @movies.current_page + 1), class: "page-link" %>
        </li>
      <% else %>
        <li class="page-item disabled">
          <span class="page-link">›</span>
        </li>
      <% end %>
    </ul>
  </nav>
</div>
  <% end %>
</div>